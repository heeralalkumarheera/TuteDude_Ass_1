TuteDude_Assignment_3
Question 1:
I created a Flask application with an /api route.
 The data is stored in a JSON file.
 When the route is accessed, the data is read and returned in JSON format.

Question 2:
I created an HTML form.
 When the form is submitted, data is sent to Flask backend.
 The backend stores the data in MongoDB Atlas.
If submission is successful, user is redirected to success page.
 If there is an error, it is shown on the same page.
